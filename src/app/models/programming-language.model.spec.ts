import { ProgrammingLanguage } from './programming-language.model';

describe('ProgrammingLanguage', () => {
  it('should create an instance', () => {
    expect(new ProgrammingLanguage()).toBeTruthy();
  });
});
