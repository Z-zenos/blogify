import { ScrollColorDirective } from './scroll-color.directive';

describe('ScrollColorDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollColorDirective();
    expect(directive).toBeTruthy();
  });
});
